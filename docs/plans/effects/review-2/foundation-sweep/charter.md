# Effects Foundation Sweep: Spike Charter

This charter plans a feasibility investigation into rebuilding the effects subsystem's foundation. It supersedes the recommendation recorded in [remediation-plan.md](../remediation-plan.md) item 12 (which recommended writing a dual-row decision record and holding a prototype in reserve): the decision has been taken to investigate the alternatives directly, by prototype, at full foundation scope. The sweep also produces the evidence that remediation items 8 (one effect-spec surface and order-independent dispatch), 13 (brand-capability and wrapper-matrix scope), and 14 (wrapper-family generation) depend on, so its conclusions feed back into all four items.

This is a charter, not a findings document. It defines the designs to compare, the proof-of-concept (POC) ladder that produces the evidence, the decision gates, and the assessment rubric. The pre-implementation decisions are settled and recorded as setup steps in the Implementation Steps section; no POC code has been written yet. POC findings will land as sibling documents in this directory and their conclusions will be folded back into the relevant remediation items.

## Project Principles

- API-breaking changes are acceptable when they lead to a better end state.
- The project prioritises correctness, internal coherence, technical debt reduction, maintainability and long-term architecture over compatibility shims for existing implementation details.
- When a local compatibility-preserving fix conflicts with a cleaner architecture, choose the cleaner architecture unless a concrete limitation prevents it.
- If such a limitation forces a fallback, document the limitation, the trade-offs, and the fallback before adopting it.

## Documentation Protocol

This plan intentionally does not use a separate section for previously resolved decisions. Resolved or adopted decisions should be folded into the implementation plan as concrete steps. This keeps the document from accumulating stale decision history while still making the intended work clear.

During implementation, this document should be kept up to date by indicating, for each of the implementation steps and POC steps, what their current status is. As each POC completes, its row in the ladder is updated (status plus a one-line outcome and a link to its findings document), each decision gate records the choice taken and why, and any conclusion that settles a remediation item is folded back into that item.

## Why a foundation sweep, and why now

The four axes below are entangled: each is foundational, each is currently implemented in a way the review flagged as costly, and a rebuild of any one touches the code the others live in. Investigating them together avoids re-deriving the same machinery three times and avoids committing to a row design before knowing whether the higher-order and substrate decisions that build on it are feasible.

The single most important grounding finding, which reframes the feasibility prior, is that the library already defunctionalises rank-2 natural transformations: [natural_transformation.rs:97](../../../../../fp-library/src/classes/natural_transformation.rs#L97) defines `NaturalTransformation<F, G>` as a trait whose method `fn transform<'a, A: 'a>(&self, fa: F::Of<A>) -> G::Of<A>` is generic over `A`, and [free.rs:1218](../../../../../fp-library/src/types/free.rs#L1218) threads it through `fold_free`. Heftia's higher-order machinery needs exactly this shape (`hfmap :: (m ~> n) -> e m ~> e n`), which stable Rust cannot express as a closure but can express as a struct with a generic method. So the deepest theoretical risk in a faithful unified-row port is already solved in this library's idiom; the remaining risks are the more mundane ones the POCs below target.

Other grounding facts confirmed before writing this charter:

- Multi-arity `Kind`s are first-class: [bifunctor.rs:89](../../../../../fp-library/src/classes/bifunctor.rs#L89) and the scoped boundary code both use `Of<'a, A, B>`. A carrier-parameterised effect kind is therefore expressible.
- There is no existing type-level order or boolean marker convention in `fp-library/src`. The order classification machinery is genuinely new code and is the subject of the first POC.

The `data-effects` package (heftia's effect-definition and core-class layer) is now available as a local reference, and reading its source corrected and sharpened several assumptions. The relevant facts, from `data-effects-core/src/Data/Effect.hs`, `Data/Effect/HFunctor.hs`, and `Data/Effect/OpenUnion.hs`:

- An effect has kind `(Type -> Type) -> Type -> Type` (it is parameterised by the carrier `f` and the result). Each effect carries three independent type-family classifications, not one: `OrderOf e :: EffectOrder` (`FirstOrder` or `HigherOrder`), `FormOf e :: EffectForm` (`Polynomial` or `Exponential`), and `LabelOf e` (a per-effect label type used as the dispatch and tagging key). Supporting classes are `FirstOrder`, `HFunctor`, `PolyHFunctor`, and `Weave`.
- `FirstOrder e` is formally `OrderOf e ~ 'FirstOrder` plus `forall f g a. Coercible (e f a) (e g a)`: a first-order effect's representation does not depend on the carrier, so it can be retyped across carriers for free. Heftia exploits this with `coerceFOEs = unsafeCoerce` gated by a `FOEs es` class (all remaining effects first-order). The Rust analog is stronger: a first-order effect brand whose `Of` projection ignores the carrier parameter makes the FOE retype a safe type-level identity, with no `unsafe` needed where Haskell needs `unsafeCoerce`. This is a green flag for Axis 1 and for `expand` over a unified row.
- `HFunctor` is `hfmap :: (forall x. f x -> g x) -> ff f a -> ff g a`, exactly the defunctionalised `NaturalTransformation` shape the library already has. Higher-order effects implement it (for example `Local f a` maps to `Local f (phi a)`).
- `FormOf` separates polynomial higher-order effects (the carrier appears only in covariant field positions; `Local`, `WriterH`'s `Listen`/`Censor`, `Catch` are all polynomial) from exponential ones (the carrier appears in a function or negative position, for example continuation-capturing effects). `PolyHFunctor` is the safety property that prevents resources escaping a scope. Polynomial higher-order effects are the mechanical case for elaboration and weave; exponential effects (CC/Shift and unlift-style operations) are out of scope for this sweep and remain a later design round.
- The open union `Union es f a` stores an integer membership index plus a runtime order tag and dispatches by index match, and the membership constraint `type e :> es` is resolved by `LabelOf` (label-keyed), not by position. So heftia's own dispatch is label-keyed and reifies order at runtime for branching, which is direct support for Axis 3's brand-keyed direction.
- Heftia's primary interpret loop (`heftia/src/Control/Monad/Hefty/Interpret.hs`) requires `FOEs es` on the remainder and uses `coerceFOEs` to relay unmatched first-order effects; the `Weave` class is a separate mechanism, explicitly the "Effect Handlers in Scope" weave used by `fused-effects` and `polysemy`, for threading a context functor through a higher-order scope with mtl-like (order-dependent, conditionally transactional) semantics. Heftia therefore offers two higher-order mechanisms, not one, which changes Axis 2 below.

## The four axes under investigation

Axis 1: Row representation. Current: dual rows, `Run<R, S, A>` with a first-order row `R` and a scoped row `S`, dispatched by the `Node` enum ([architecture.md](../architecture.md) section 1, section 3.1). Alternative: a single unified row `E` of effect brands, each carrying an order marker, matching the referenced heftia's current design. Cost of the status quo: every program type and alias and error carries two row parameters; first-order-only programs (the common case) drag `CNilBrand`/`ScopedNil` everywhere; row machinery (`expand`, `weaken`, interpose, narrowing) exists in first-order and scoped variants.

Axis 2: Higher-order effect (HOE) mechanism. This is a three-way choice, sharpened by the `data-effects` reading. The current local mechanism is boundary frames plus per-wrapper carrier and residual dispatch protocols, the largest single source of internal complexity ([architecture.md](../architecture.md) section 1 item 5, and the W8 audit). Heftia offers two alternatives, both of which a unified row enables and both of which would delete the boundary-frame subsystem and most of the five result-polymorphic protocol traits:

- Elaboration: rewrite each HOE into first-order effects (via the existing interpose machinery) before algebraic handling, requiring an `FOEs`-style "remainder is all first-order" property at the point of algebraic handling. This is the cleaner, more predictable mechanism (interpretation stays a structural rewrite, order-independent for the cases it covers), and is heftia's primary interpret path.
- Weave: thread a context functor through the higher-order scope ("Effect Handlers in Scope", the `fused-effects`/`polysemy` approach, shipped in `data-effects` as the `Weave` class). It handles strictly more (it does not require elaborating the HOE away first) at the cost of mtl-like, order-dependent semantics. Notably, that order-dependence is exactly the branch-local-versus-global behaviour the review flagged as currently inexpressible with the cell-based runners ([refactoring-opportunities.md](../refactoring-opportunities.md) R5): weave is one principled way to obtain it, so this axis and the R5 semantics gap are connected.

Elaboration is the architectural prize and the primary bet; weave is the heftia-blessed fallback for higher-order effects that resist clean elaboration, and is itself strictly better than retaining boundary frames (it is the documented upstream mechanism rather than a bespoke local one). The sweep scopes both to polynomial higher-order effects; exponential effects (CC/Shift, unlift) are a separate later design round.

Axis 3: Membership and handler dispatch. Current: positional cons-lists walked in lock-step with the row, requiring the row and handler macros to sort entries by a syntactic structural key, which produces the spelling-sensitive footgun ([refactoring-opportunities.md](../refactoring-opportunities.md) R1, item 8). Alternative: brand-keyed type-level dispatch (search the handler list by effect brand), which makes handler-list order irrelevant and removes the handler-side sort. A unified row interleaves first-order and higher-order effects by sort key, which makes positional alignment strictly worse, so brand-keyed dispatch is likely near-mandatory for a unified row rather than merely desirable. This is also the heftia-faithful choice: heftia's membership constraint `type e :> es` is resolved by `LabelOf` (label-keyed), and the same label mechanism is how heftia expresses tagged duplicate effects. Brand-keyed dispatch therefore unifies remediation items 8 (the footgun) and 11 (tagged effects): the effect brand is the natural label, and a tag is a wrapper that changes the label.

Axis 4: Substrate and closure storage. Current: six hand-maintained wrapper families over six substrates (erased and explicit, each Box/Rc/Arc), 60 to 75 percent mutually identical, plus per-effect brand siblings keyed by closure storage ([architecture.md](../architecture.md) section 3.6; [refactoring-opportunities.md](../refactoring-opportunities.md) R2, R3). Alternative: one substrate parameterised by a closure-storage brand, with one brand per effect parameterised the same way. The wrapper-matrix table in `types/effects.rs` records that Brand-level class unification is blocked by per-result `Clone` (Rc) and `Send + Sync` (Arc) projection bounds that trait method signatures cannot carry; the hypothesis this sweep tests is that a concrete parameterised substrate can carry those bounds on its inherent methods (which can have per-call where-clauses that trait methods cannot), even though the Brand-level classes cannot.

A fifth concern, the result-polymorphic handler protocol (the five `RunFirstOrder*` traits), is treated as a consequence rather than an axis: those traits exist to serve boundary-frame result polymorphism, so if Axis 2 resolves toward elaboration most of them disappear without separate investigation.

## Candidate end-state designs

These are coherent points in the cross-product of the axes, used as the targets the POCs evaluate. The baseline is included so the rubric has a control.

- FS-0 (baseline, control). The current design: dual rows, boundary frames, positional dispatch, six wrappers. No work; scored only for comparison.
- FS-1 (north star). Unified row + elaboration + brand-keyed dispatch + single parameterised substrate. Maximum debt reduction (deletes boundary frames, the protocol traits, the scoped-row machinery, and the six-wrapper duplication; collapses the brand siblings). Maximum risk: every axis must resolve favourably.
- FS-2 (pragmatic hybrid). Unified row + brand-keyed dispatch + single parameterised substrate, with higher-order effects handled by weave (heftia's `Weave`) rather than elaboration. The fallback if elaboration (Axis 2) fails for some higher-order effects but the row, dispatch, and substrate unifications hold. Weave still deletes the boundary-frame subsystem (it is the upstream mechanism, not a bespoke one) and accepts mtl-like order-dependent semantics, which doubles as a principled route to the R5 branch-local-versus-global behaviour. Retaining today's boundary frames under a unified row is the further fallback (FS-2b) only if both elaboration and weave prove infeasible.
- FS-3 (facade stepping stone). Unified user-facing surface via a type-level partition that splits the one row back into today's `Node<FO, HO>` internally; everything else unchanged. Lowest risk, lowest reward: delivers the signature-noise win and a single row parameter immediately while deleting no internals. Useful as an incremental ship even if FS-1/FS-2 are later adopted.

The substrate unification (Axis 4) is separable: it can be adopted with any of FS-1/FS-2/FS-3, or deferred. The POCs keep it isolated (Tier E) so the row decision does not depend on it.

## POC ladder

Principle: isolate each load-bearing risk as the smallest prototype that answers a specific question, run the cheap shared risks first, gate before the expensive bets, and only build an end-to-end slice for the design that survives. All POCs target the default single-wrapper path first to isolate the row, dispatch, and HOE questions from the substrate axis; the substrate axis is tested separately in Tier E. The POC harness, file location, crate-access level, Coyoneda convention, baseline capture, semantic-validation convention, and git hygiene are fixed by the setup steps in the Implementation Steps section. None of the POC code is production code; all of it is removed or promoted after the decision, while the per-POC findings documents are the durable artifacts.

| POC    | Tier             | Goal (question it answers)                                                                                                                                                                                                                                                              | Pass criteria                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | Informs               | Status                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| ------ | ---------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| POC-0  | A classification | Can each effect brand carry an order marker, and can a recursive `AllFirstOrder` constraint over a coproduct reject a row containing a HOE with a readable error?                                                                                                                       | Compiles on stable; a `trybuild` case shows the rejection names the offending effect comprehensibly.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | Axes 1, 2             | Pass: see [poc-0-findings.md](poc-0-findings.md). Classification works over the real `CoproductBrand` row; the higher-order rejection names the offending effect via `#[diagnostic::on_unimplemented]` when first-order membership is a directly-implemented marker (E0277) rather than an associated-type blanket (E0271).                                                                                                                    |
| POC-1  | A classification | Can a type-level `Partition<Coproduct>` split a unified row into first-order and higher-order sub-rows and round-trip a value through it?                                                                                                                                               | Total and stable; lifetimes and `Member` indices preserved across the split and reinjection.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | FS-3 (Axis 1)         | Pass: see [poc-1-findings.md](poc-1-findings.md). A marker-keyed `PartitionPlace` helper splits the brand row totally and stably (no overlapping impls), preserving relative order; the value round-trip rides frunk `subset`/`embed`. FS-3 is feasible.                                                                                                                                                                                       |
| POC-2  | B dispatch       | Can handlers be dispatched by effect brand via type-level search over a coproduct, so handler-list order is irrelevant?                                                                                                                                                                 | A mixed two-effect row is handled with handler entries in arbitrary order, no positional sort needed; on a wider multi-effect row, inference resolves without turbofish in the common case and a missing-handler error names the missing brand legibly (feeds rubric item 3).                                                                                                                                                                                                                                                                                                                                                                                                                                 | Axis 3, item 8        | Pass: see [poc-2-findings.md](poc-2-findings.md). Handler-list order is irrelevant (brand-keyed `HandleByBrand` search); the row walk uses the frunk Sculptor index-list pattern (a single free index is E0207); the missing-handler error names the brand via `on_unimplemented`. Cures item 8's sort footgun.                                                                                                                                |
| POC-3  | C unified row    | Can one row hold both a first-order and a higher-order effect with `Functor`/`WrapDrop` impls and an order-directed `peel`, and drop a deep chain without overflow?                                                                                                                     | Compiles; deep-chain drop test passes; peel dispatches both kinds.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | Axes 1, 2             | Pass: see [poc-3-findings.md](poc-3-findings.md). Over the real `Free` substrate, a uniformly Coyoneda-wrapped row holds both kinds (effects are `impl_kind!`-only); peel classifies the active arm by order; realistic queue-deep programs drop safely. Shared non-blocking limitation: deep `Wrap`-nesting of Coyoneda cells is not drop-safe under either design.                                                                           |
| POC-4  | D HOE prize      | Can a same-result HOE (`Catch`, action result equals operation result) be elaborated into first-order `Throw` plus interpose, reproducing boundary-frame semantics?                                                                                                                     | Matches the existing State-with-Catch ordering results in [run_heftia_semantics.rs](../../../../../fp-library/tests/run_heftia_semantics.rs).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | Axis 2 (FS-1)         | Pass (elaboration arm): see [poc-4-findings.md](poc-4-findings.md). `catch` elaborated into an interpose over `Throw` reproduces the state-write-survives-catch result `(true, true)` on the real `Free` substrate, no boundary frames; state writes survive automatically because the interpose only rewrites `Throw`.                                                                                                                        |
| POC-5  | D HOE prize      | Can a result-shape-changing HOE (`listen`, where the action result `a` differs from the operation result `(w, a)`) be stored as an in-row cell and elaborated by an interpret pass, without boundary frames? (`censor` is same-result, the easy half; `listen` is the discriminator.)   | An in-row higher-order `listen` cell is elaborated by an interpret pass (no boundary frames, no result-polymorphic protocol traits, no scoped row) to reproduce the single-Writer result-shape-changing result: `listen(tell >> tell >> pure(a))` yields `(w, a)` (the boundary-split suite's `(42, "firstsecond")`). The action result may be a brand type parameter (full existential hiding is blocked by an object-safety constraint orthogonal to the gate, documented in step E2); construction-time elaboration that never forms a row cell does not satisfy this gate. Make-or-break for deleting the boundary subsystem.                                                                             | Axis 2 (FS-1 vs FS-2) | Pass (elaboration arm): see [poc-5-findings.md](poc-5-findings.md). An in-row `listen` cell (action result a brand type parameter) elaborated by a plain interpret pass reproduces `(42, "firstsecond")` with the tells propagated, no boundary frames, no result-polymorphic protocol traits, no scoped row. FS-1 (elaboration) selected; the result-shape change is carried by the cell's continuation type, recovered by `Coyoneda::lower`. |
| POC-6  | D HOE prize      | Can row widening (`expand`) over the unified row, including higher-order cells carrying sub-programs, be done via the defunctionalised `NaturalTransformation` used as `hfmap`, over the public `hoist_free`/`fold_free` (no raw-step path, since gate G2 deleted the boundary frames)? | A program containing a `listen` cell widens from a row `R1` into a larger row `R2` and still evaluates to the same result (`(42, "firstsecond")`). The higher-order cell stores its action behind an `hfmap`-capable abstraction so widening rebuilds the action for `R2` (the adopted Approach (c); a row-hardcoded cell cannot retype its action and a carrier-parameterised brand is a type-alias cycle). The first-order coproduct widening is manual `Inl`/`Inr` re-injection, generic over the hole, avoiding the unexpressible `for<A>` embedding bound. Documented fallback if the `hfmap` cell is infeasible in stable Rust: widen first-order-only programs and record the higher-order limitation. | Axes 1, 2             | Not started                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| POC-7  | E substrate      | Can one concrete substrate parameterised by a closure-storage brand carry the per-result `Clone` (Rc) and `Send + Sync` (Arc) bounds on its inherent `pure`/`bind`/`map`/`lift`/`handle` methods, replacing the six-wrapper duplication for a minimal slice?                            | A single `Run<Store, E, A>` compiles and runs one effect end-to-end for `Store in {Box, Rc, Arc}`; the Arc instantiation is `Send + Sync`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | Axis 4, items 13, 14  | Not started                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| POC-8  | E substrate      | Can one `ClosureStorage` class unify `ToDynFnOnce`/`ToDynCloneFn`/`ToDynSendFn` (reconciling FnOnce consumption against Fn cloning) behind one associated stored-closure type, collapsing the per-effect brand siblings?                                                                | A minimal State-like effect is defined once over the storage brand and works for all three; or the FnOnce/Fn split is shown to block it, with the exact failure recorded for the fallback prefix scheme.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | Axis 4 (R3)           | Not started                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| POC-9  | F integration    | Does the surviving design carry a real slice (State + Reader + Catch + Writer-censor) end-to-end?                                                                                                                                                                                       | The slice compiles and passes the applicable heftia semantics cases on the chosen row/dispatch/substrate.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | overall               | Not started                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| POC-10 | F integration    | What is the per-layer dispatch and allocation shape of the surviving design versus FS-0?                                                                                                                                                                                                | A micro-benchmark shows dispatch cost per layer and allocations per bind are no worse than FS-0, or the regression is quantified and judged acceptable.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | overall               | Not started                                                                                                                                                                                                                                                                                                                                                                                                                                    |

POC-4 and POC-5 each carry two arms, run in preference order: an elaboration arm (rewrite the higher-order effect into first-order effects, the FS-1 mechanism) and, if elaboration cannot reproduce the semantics, a weave arm (thread a context functor through the scope via the defunctionalised `NaturalTransformation`, the heftia `Weave` mechanism, the FS-2 mechanism). POC-5's result-shape-changing case (`listen`, where the action result `a` differs from the operation result `(w, a)`; `censor` is same-result and rides along only as a check) is the one that discriminates the arms, because it is precisely what forced today's boundary/carrier split, and POC-5 must exhibit it as an in-row cell (see step E2) rather than as a construction-time rewrite, so the gate decision transfers to the production design. Both arms target polynomial higher-order effects only; exponential effects are out of scope here.

## Decision gates

- G1, after Tier A, B, C (the cheap, shared POCs). Decide: is FS-3 (facade) viable as an immediate increment, and do the unified row plus brand-keyed dispatch hold well enough to pursue FS-1/FS-2? If POC-0 or POC-3 fails outright, the unified row is infeasible and the sweep falls back to keeping dual rows (and item 12 reverts to its approach A, the decision record). If POC-1 passes but Tier D later fails, FS-3 is the shippable result.
  - Outcome (reached): proceed to Tier D, and FS-3 is confirmed available as a fallback. All four Tier A POCs passed: classification ([poc-0-findings.md](poc-0-findings.md)), partition ([poc-1-findings.md](poc-1-findings.md)), brand-keyed dispatch ([poc-2-findings.md](poc-2-findings.md)), and the unified row over the real `Free` substrate with order-directed peel and realistic deep-drop safety ([poc-3-findings.md](poc-3-findings.md)). The unified row and brand-keyed dispatch hold on the real encoding, and brand-keyed dispatch cures item 8's footgun, so the evidence supports pursuing the full unified row (FS-1/FS-2) rather than stopping at the facade. Decision taken (the FS-3-as-interim question): FS-3 is not shipped as an interim increment. Per the project principles, the facade deletes no internals and would itself be replaced, so the short-term signature-noise relief does not justify shipping a stepping stone that adds migration churn; FS-3 is held only as the fallback to ship if Tier D fails (POC-1 proved its split, so it remains available). One shared, non-blocking limitation was recorded (deep `Wrap`-nesting of Coyoneda cells is not drop-safe under either the current or unified design; realistic queue-deep programs are safe). The reasoning follows the project principles: the cheap tier showed no concrete limitation forcing the dual row, so the cleaner unified architecture is pursued.
- G2, after POC-5 (the critical gate). Decide the higher-order mechanism in order of preference: elaboration (FS-1), then weave (FS-2), then retained boundary frames (FS-2b). If POC-4 and POC-5 reproduce the heftia semantics by elaboration, FS-1 is on the table and the boundary subsystem is deletable. If elaboration fails for the result-shape-changing case (`listen`/`censor`), POC-5's weave arm decides whether weave reproduces the semantics instead; if it does, the sweep targets FS-2 (still deleting boundary frames). Only if both fail is the limitation recorded per the project principles and boundary frames retained under the unified row (FS-2b).
  - Outcome (reached): elaboration (FS-1) selected. Both higher-order cases the sweep targets reproduce the heftia semantics by elaboration over the unified row with no boundary frames: POC-4 (same-result `Catch`, [poc-4-findings.md](poc-4-findings.md)) and POC-5 (the result-shape-changing `listen`, [poc-5-findings.md](poc-5-findings.md), which yields `(42, "firstsecond")` with the tells propagated). The weave arm was not needed for either. So the boundary-frame subsystem and the result-polymorphic protocol traits are deletable, and the sweep targets FS-1; weave (FS-2) is not the primary mechanism and remains available only for the later exponential-effect round. The result-shape change that forced today's boundary/carrier split is carried instead by the listen cell's own continuation type (recovered by `Coyoneda::lower`), with the action result a brand type parameter (the object-safety constraint documented in step E2, no worse than the current design). The reasoning follows the project principles: elaboration is the cleaner mechanism and no concrete limitation forces the boundary frames, so the expensive machinery is removed. POC-6 (`expand` over the unified row) remains as the final Tier D feasibility item but does not gate the higher-order-mechanism choice.
- G3, after Tier E. Decide substrate unification go/no-go, which settles remediation item 13's approach C (can the matrix collapse) and rescopes item 14 (what, if anything, still needs generation). Independent of G2.
- G4, after Tier F. Write the overall recommendation back into remediation items 8, 12, 13, and 14, with the rubric scores and the chosen design.

## Assessment rubric

Each surviving design is scored against FS-0 on:

1. Builds on stable Rust with no nightly features.
2. Higher-order semantics correct on the heftia suite (especially the listen/censor and State-with-Catch ordering cases).
3. Signature noise: does a first-order-only program shed the second row parameter; typical error-message length for a missing handler.
4. Machinery line-count delta versus FS-0: does it delete the boundary-frame subsystem, the protocol traits, the scoped-row machinery, the six-wrapper duplication, the brand siblings.
5. Migration cost: how much of the effect catalog, the macros, and the wrapper matrix must change.
6. Performance shape: per-layer dispatch overhead and per-bind allocation versus FS-0 (POC-10).
7. Effect on the positional-sort footgun (item 8): eliminated, improved, or unchanged.
8. Composability of the axes: does adopting one axis force or preclude another.

## Open Questions, Decisions, Issues and Blockers

All items previously tracked here have been adopted into concrete steps and gate records, following the project principles (cleaner end state, scope limitations documented rather than left implicit). The result-shape-changing-HOE design scope and cell encoding are fixed in execution step E2 and POC-5's pass criteria (the action result is a brand type parameter; full existential hiding is blocked by a Rust object-safety constraint orthogonal to the gate, documented in E2); the FS-3-as-interim question is decided in the gate G1 outcome (not shipped); and the polynomial-versus-exponential effect catalogue is execution step E5.

The POC-6 row-widening design (carrier-polymorphism of higher-order cells for `expand`) is fixed in POC-6's pass criteria and execution step E2: the higher-order cell stores its action behind an `hfmap`-capable abstraction (Approach (c), the heftia `HFunctor` shape) so widening can rebuild the action for the larger row, with manual coproduct injection over the public `hoist_free`/`fold_free` and a documented first-order-only fallback. The W1 spike's two rejection reasons are reconciled there (its boundary-frame reason removed by gate G2; its embedding-evidence reason sidestepped by manual injection).

The genuinely-open unknowns that remain are gate outcomes, tracked in the Decision gates section: substrate unification at G3 and the overall recommendation at G4. Gate G2 (the higher-order mechanism) is now decided as elaboration / FS-1. Each remaining gate resolves when its tier completes.

## Implementation Steps

The setup and execution steps below adopt the approach for each former open-questions item that best fits the Project Principles (cleaner architecture and debt reduction over short-term convenience, with limitations documented): the setup steps settle the pre-implementation conventions and must complete before Tier A; the execution steps run the POC ladder and decision gates above in tier order, and also carry the adopted result-shape-changing-HOE scope (E2) and the polynomial-versus-exponential catalogue (E5). Status is tracked per step and, for POCs, in the POC ladder table.

### Setup

- S1. Spike branch and worktree (from former item on git hygiene). Create a dedicated `spike/foundation-sweep` git worktree for all POC code and run `direnv allow` inside it. Only the durable artifacts (this charter and the per-POC findings documents) are committed to `feat/effects`; POC code stays on the spike branch and is cherry-picked into production only if it survives the gates. Code promoted from the spike to production must meet the full `just verify` gate and `#[document_module]` coverage as part of that promotion. Reasoning: keeps mainline history free of throwaway prototypes while preserving the work, matching the global worktree and preserve-exploratory-work guidance. Status: done; worktree at `.claude/worktrees/foundation-sweep` on branch `spike/foundation-sweep`, direnv allowed.
- S2. POC harness conventions, fidelity, and verification scope (adopts the harness, foundational-machinery-fidelity, and lint-scope decisions). Type-system-technique POCs (POC-0, POC-1, POC-2) are standalone, meaning independent of the effects subsystem (no `Run`, `Node`, boundary frames, or handlers) while still building on the library's real foundational primitives: the `Kind`/`Apply!`/`impl_kind!` machinery, the `frunk_core` coproduct behind `CoproductBrand`, and the closure-storage classes. This proves techniques against the actual encoding, including its GAT-normalization quirks, rather than a simplified stand-in that risks a non-transferable false positive; if a technique genuinely cannot be exercised against the real primitives in isolation, hand-roll the minimum and mark that result as needing re-validation against the real encoding before it can pass a gate. Substrate-composition POCs (POC-3, POC-6, POC-7, POC-8) are public-API integration tests restricted to `Free::{lift_f, resume, fold_free, hoist_free, bind, map, pure}` and `classes::pointer`; a POC drops to an in-crate unit test only if it provably cannot be expressed over the public API, and that necessity is recorded as a finding about the public substrate surface. `trybuild` (a dev-dependency in both crates) backs the compile-fail error-quality cases. POC code is throwaway: it need only compile and pass its assertions and `trybuild` cases while on the spike branch, and is exempt from `#[document_module]` coverage and the full `just verify` gate (which apply only at promotion, per S1). Reasoning: matching each probe to the smallest harness keeps the cheap tier decoupled from the 35k-line system; proving techniques against the real encoding avoids a feasibility spike's worst failure mode (a green result that does not transfer); and exempting throwaway code from production documentation rules follows the debt-reduction principle. Status: not started.
- S3. Coyoneda convention for the feasibility tier (from former item on Coyoneda wrapping). Tiers A through D fix uniform Coyoneda wrapping of every effect, matching the proven existing approach, to remove a variable. Whether the production design drops Coyoneda (bare coproduct plus generated `Functor`/`HFunctor` impls) is decided in the integration tier from POC-10's allocation measurements, not now. Reasoning: the wrapping choice is a performance/boilerplate trade, not a feasibility blocker, so pinning it to the known-good option keeps the early POCs focused on the genuine unknowns and defers the production choice to evidence. Status: not started.
- S4. Prior-spike reconciliation (from former item on the W1/W8 spikes). Before Tiers D and E, re-read `review-1/w1-row-embed-spike.md` and `review-1/w8-consolidation-feasibility.md`. Frame POC-6 around the falsifiable hypothesis that a unified, boundary-frame-free row removes W1's raw-step requirement, making `expand` expressible over the public `fold_free`/`hoist_free`; frame POC-7 against W8's measured 83-percent-mechanical finding. Reasoning: avoids redoing settled analysis and turns the unified row's central simplification into concrete evidence rather than rediscovering the dual-row result. Status: W1 re-read done for POC-6 (its two rejection reasons and the framing are folded into POC-6's pass criteria and execution step E2: reason (2), boundary frames, is removed by gate G2; reason (1), the embedding evidence, is sidestepped by manual injection; and the carrier-polymorphism of higher-order cells is handled by the `hfmap` cell of Approach (c)). W8 re-read pending before POC-7.
- S5. Semantic-validation convention (from former item on standalone-prototype validation). Higher-order POCs (POC-4, POC-5, POC-9) restate the concrete expected values from `run_heftia_semantics.rs` (the State-with-Catch ordering pair, the listen/censor pre/post strings) as inline assertions, each citing the mirrored suite case, keeping the prototypes self-contained per the project documentation rule. Reasoning: the expected values are small and stable; inline assertions keep the POCs fast and decoupled while preserving traceability, and avoid coupling prototypes to the production `Run` API. Status: not started.

### Tier 0: baseline capture

- S6. Capture the FS-0 baseline metrics (from former item on baseline capture). Record the line counts of the boundary-frame subsystem, the five result-polymorphic protocol traits, and the six-wrapper duplication (with the measurement commands), and run the existing `effect_rows` and `scoped_operations` benches to fix baseline numbers. Reasoning: the rubric's debt-reduction and performance rows depend on these numbers; capturing them once now makes every later comparison concrete and refreshes the W8 line counts remediation item 14 needs. Status: line counts done ([baseline-fs0.md](baseline-fs0.md)); the perf benches are deferred to immediately before POC-10 (recorded in the baseline doc), since those numbers only feed the POC-10 comparison and would otherwise drift.

### Execution

Run the POC ladder and decision gates above in tier order; per-POC pass/fail detail lives in the POC ladder table and gate outcomes in the Decision gates section. Each tier's findings are written up as a sibling document before the next tier begins.

- E1. Tiers A, B, C (POC-0, POC-1, POC-2, POC-3), then decision gate G1. Status: done. All four POCs passed ([poc-0-findings.md](poc-0-findings.md), [poc-1-findings.md](poc-1-findings.md), [poc-2-findings.md](poc-2-findings.md), [poc-3-findings.md](poc-3-findings.md)); gate G1 reached and recorded in the Decision gates section (proceed to Tier D; FS-3 confirmed available as a fallback).
- E2. Tier D (POC-4, POC-5, POC-6), then decision gate G2. Each higher-order POC runs its elaboration arm first and the weave arm only if elaboration cannot reproduce the semantics. POC-5 is built as an in-row `listen` cell: a higher-order row cell whose `Listen` variant carries the action result as a brand type parameter, `ListenBrand<W, RAction>` with `Of<'a, Next> = ListenCell<'a, W, RAction, Next>`, Coyoneda-wrapped and given a `Functor` as in POC-4, elaborated by an interpret pass that accumulates and re-emits tells and resumes with `(w, a)`, asserted on the single-Writer case `listen(tell >> tell >> pure(a))` yielding `(w, a)` (the boundary-split suite's `(42, "firstsecond")`). The action result is a brand type parameter rather than existentially hidden because hiding it is blocked in stable Rust: the unified row Coyoneda-wraps every cell (S3), so the interpret pass must call `Coyoneda::lower` ([coyoneda.rs:447](../../../../../fp-library/src/types/coyoneda.rs#L447)), which is bounded `where F: Functor`, and `Functor::map` ([functor.rs:127](../../../../../fp-library/src/classes/functor.rs#L127)) must post-compose onto the continuation `k: (W, RAction) -> Next`; if `RAction` were erased behind a trait object that post-composition would need a generic method on the trait object, which is not object-safe. This limitation is orthogonal to gate G2 (whether the boundary subsystem can be deleted), costs only a residual type parameter that is no worse than today's design (which carries it too, in `WriterListenBrand<P, W, A>`), and does not reintroduce the expensive carrier-split machinery (boundary frames, the result-polymorphic protocol traits, the scoped row), because elaboration is a plain interpret pass over the one row. Full existential hiding is left as a non-gating refinement: if a safe encoding is later found it is an ergonomic improvement, not a gate condition. Construction-time elaboration (a call-site rewrite that never forms a row cell) is recorded only as a weaker fallback signal if the in-row interpret pass is blocked, because a green result there would not transfer to the in-row production design. Status: in progress; POC-4 and POC-5 both passed via elaboration ([poc-4-findings.md](poc-4-findings.md), [poc-5-findings.md](poc-5-findings.md)); gate G2 reached and recorded (FS-1 selected, boundary subsystem deletable). POC-6 (`expand`) is the remaining Tier D item, built per the adopted Approach (c): the higher-order cell stores its action behind an `hfmap`-capable abstraction (the heftia `HFunctor` shape, using the library's defunctionalised rank-2 `NaturalTransformation`), so widening applies a row-widening NT to the action and rebuilds the cell for the larger row. This breaks the type-alias cycle that a carrier-parameterised brand (`ListenBrand<Carrier, RAction>` where `Carrier = Free<Row, _>` and `Row` contains that brand) cannot. The first-order coproduct widening is manual `Inl`/`Inr` re-injection rather than frunk's bounded `embed`, because manual injection is generic over the hole and sidesteps the unexpressible `for<A>` embedding bound (the W1 spike's first rejection reason); the whole thing runs over the public `hoist_free`/`fold_free`, testing the hypothesis (setup step S4) that the boundary-frame-free row (gate G2) needs no raw-step path (the W1 spike's second rejection reason, now removed). If the `hfmap` cell proves infeasible in stable Rust, the documented fallback is to widen only first-order-only programs and record the higher-order limitation (a strict subset of today's capability) per the project principles.
- E3. Tier E (POC-7, POC-8), then decision gate G3. POC-7 produces the wrapper-matrix evidence remediation item 13 needs rather than waiting on item 13's decision, and targets the current full wrapper set unless item 13 has already reduced it. Status: not started.
- E4. Tier F (POC-9, POC-10), then decision gate G4: fold the rubric scores and the chosen design back into remediation items 8, 12, 13, and 14. Status: not started.
- E5. Catalogue the polynomial-versus-exponential split of the effect set (from the former open item on which effects `data-effects` marks exponential). At the G4 handoff, read the `data-effects` effect modules and record, as a short note in this directory, which effects are polynomial (the carrier appears only in covariant field positions, so elaboration and weave apply) versus exponential (the carrier appears in a function or negative position, for example CC/Shift and unlift-style operations). This bounds the later exponential design round, which is out of scope for this sweep. Reasoning: the sweep deliberately scopes to polynomial effects, so making the boundary explicit and recorded prevents the exponential cases from being silently assumed in scope and gives the later round its starting catalogue; per the project principles, the scope limitation is documented rather than left implicit. Status: not started (handoff deliverable at G4).

### Status summary

- Charter: drafted and reviewed; pre-implementation decisions adopted as setup steps S1 through S6.
- Setup: S1 done (spike worktree); S2 through S5 are standing conventions in force; S6 line counts done ([baseline-fs0.md](baseline-fs0.md)), perf baseline deferred to before POC-10.
- Execution: E1 done (Tier A); E2 (Tier D) in progress, POC-4 and POC-5 passed via elaboration ([poc-4-findings.md](poc-4-findings.md), [poc-5-findings.md](poc-5-findings.md)), gate G2 reached (FS-1 selected); POC-6 (`expand`) is the remaining Tier D item. E3, E4, and E5 (the G4-handoff polynomial/exponential catalogue) not started.
- Decision gates: G1 reached (proceed to Tier D; pursue FS-1/FS-2; FS-3 not shipped as an interim, held only as the Tier-D-failure fallback). G2 reached (elaboration / FS-1 selected; boundary subsystem deletable; weave held only for the later exponential round). G3 and G4 not reached.
- Open questions: none currently open; all adoptable items folded into steps (E2, E5) and the G1 gate record, with the remaining unknowns being the G2 through G4 gate outcomes.
