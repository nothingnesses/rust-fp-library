# Effects System Review, 2026-05-16

## Scope

This review covers the current effects implementation in:

- [`fp-library/src/brands/effects.rs`](../../../../../fp-library/src/brands/effects.rs).
- [`fp-library/src/types/effects.rs`](../../../../../fp-library/src/types/effects.rs) and submodules.
- [`fp-macros/src/effects.rs`](../../../../../fp-macros/src/effects.rs) and submodules.

It also compares the current surface against local reference copies of:

- `/home/jessea/Documents/projects/purescript-run/src/Run`.
- `/home/jessea/Documents/projects/effects/heftia/heftia-effects/src/Control/Monad/Hefty`.

## Generated Inventory

The item inventory lives in [`item-inventory.md`](item-inventory.md). It was
generated by [`collect_effect_items.py`](collect_effect_items.py), which uses
`rust-analyzer symbols` to parse Rust AST symbol output, compute source line
numbers from byte ranges, and seed the description column from nearby Rust doc
comments.

Generation command:

```bash
direnv exec . python3 docs/plans/effects/review/3-current-effects-system-review/collect_effect_items.py > docs/plans/effects/review/3-current-effects-system-review/item-inventory.md
```

The production inventory excludes test-only files and inline `mod tests` blocks
by default. It contains 1,777 symbols:

| Kind      | Count |
| --------- | ----: |
| Module    |   129 |
| Struct    |   107 |
| Enum      |    44 |
| Variant   |    55 |
| TypeAlias |    79 |
| Trait     |    48 |
| Impl      |   449 |
| Function  |   349 |
| Method    |   517 |

The generated table is intentionally broad rather than curated. It is useful as
a map of the subsystem and as a way to find undocumented or weakly documented
items, but it is not a substitute for semantic review. Some fallback
descriptions are necessarily generic where source items have no local prose doc
comment.

## High-Level Status

The effects system is now a substantial implementation rather than a
proof-of-concept. It has:

- A dual-row `Run<R, S, A>` model where `R` is the first-order effect row and
  `S` is the scoped or higher-order effect row.
- Six wrapper families: `Run`, `RcRun`, `ArcRun`, `RunExplicit`,
  `RcRunExplicit`, and `ArcRunExplicit`.
- First-order effects for State, Reader, Except, Writer, Choose, and Empty.
- Scoped effects for Catch, Local, RefLocal, Bracket, RefBracket, Span, and
  Writer listen/censor.
- Runtime first-order and scoped handler lists.
- Standard scoped handler values for the built-in scoped effects.
- Row construction, handler construction, scoped-row definition, row-alias, and
  inherent-method do-notation macros.
- Internal continuation-carrier and boundary protocols for around-action
  scoped effects where ordinary `Functor::map` is not enough to preserve the
  action boundary and the outer continuation separately.

The core architecture is directionally sound. The library has correctly moved
away from treating scoped effects as just first-order effects with larger
payloads. Around-action effects need the selected action program and the outer
continuation queue to remain distinct until the handler chooses how to resume.
The current boundary/carrier machinery reflects that semantic need.

## Major Implemented Pieces

| Area                      | Items                                                                                                                                                                                       | Role                                                                                                 |
| ------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------- |
| Row brands                | `CNilBrand`, `CoproductBrand`, `NodeBrand`                                                                                                                                                  | Encode first-order rows, scoped rows, and the dual-row node stored in Free-family wrappers.          |
| First-order effect brands | `StateBrand`, `ReaderBrand`, `ExceptBrand`, `WriterBrand`, `ChooseBrand`, `EmptyBrand`, plus Box/Send variants where needed                                                                 | Provide `Kind` projections for ordinary operation functors.                                          |
| Scoped effect brands      | `CatchBrand`, `LocalBrand`, `RefLocalBrand`, `BracketBrand`, `RefBracketBrand`, `SpanBrand`, `WriterListenBrand`, `WriterCensorBrand`, plus Box/Send/Explicit variants where needed         | Provide `Kind` projections for around-action scoped cells.                                           |
| Row values                | `VariantF`, `Coproduct`, `ScopedCoproduct`, `ScopedNil`, `Member`, `CoproductEmbedder`                                                                                                      | Inject, project, narrow, embed, and dispatch row members.                                            |
| Runners                   | `Run`, `RcRun`, `ArcRun`, `RunExplicit`, `RcRunExplicit`, `ArcRunExplicit`                                                                                                                  | User-facing effect program wrappers over Free-family substrates.                                     |
| First-order handlers      | `Handler`, `HandlersNil`, `HandlersCons`, `nt`, `DispatchHandlers`                                                                                                                          | Runtime handler list for first-order effect rows.                                                    |
| Scoped handlers           | `ScopedHandler`, `ScopedHandlersNil`, `ScopedHandlersCons`, `scoped_nt`, `DispatchScopedHandler`, `DispatchScopedHandlers`, `DispatchScopedBoundaryHandlers`, carrier-aware dispatch traits | Runtime handler list and internal dispatch protocols for scoped rows.                                |
| Standard scoped handlers  | `catch_handler`, `local_handler`, `ref_local_handler`, `bracket_handler`, `ref_bracket_handler`, `span_handler`, `writer_pre_handler`, `writer_post_handler`                                | Built-in handlers for the built-in scoped effects.                                                   |
| Macros                    | `effects!`, `raw_effects!`, `scoped_effects!`, `handlers!`, `scoped_handlers!`, `define_scoped_row!`, `define_effect_row_aliases!`, `im_do!`                                                | Build sorted rows, handler lists, recursive scoped-row markers, row aliases, and do-notation syntax. |

## Effect Coverage

### Current First-Order Effects

| Effect | Current Rust item family                                             | Notes                                                                                                                     |
| ------ | -------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------- |
| State  | `State`, `BoxState`, `SendState`; `get`, `put` smart constructors    | Box/Rc/Arc split tracks single-shot, multi-shot, and thread-safe closure storage.                                         |
| Reader | `Reader`, `BoxReader`, `SendReader`; `ask` smart constructor         | `local` is modeled as a scoped effect rather than by changing the first-order `Reader` cell.                              |
| Except | `Except`; `throw` smart constructor                                  | `catch` is scoped so it can delimit the protected action.                                                                 |
| Writer | `Writer`; `tell` smart constructor                                   | `listen` and `censor` are scoped Writer operations with standard pre/post handler variants.                               |
| Choose | `Choose`, `BoxChoose`, `SendChoose`; `choose` on multi-shot wrappers | Smart constructors intentionally exclude single-shot default wrappers because the continuation is invoked more than once. |
| Empty  | `Empty`; `empty` smart constructor                                   | Abortive branch used by nondeterministic programs.                                                                        |

### Current Scoped Effects

| Effect               | Current Rust item family                                                                                                                                  | Notes                                                                               |
| -------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------- |
| Catch                | `BoxCatch`, `Catch`, `SendCatch`; `catch_handler`                                                                                                         | Delimits an action and recovers from matching `Except` throws inside that action.   |
| Local                | `BoxLocal`, `Local`, `SendLocal`; `local_handler`                                                                                                         | Applies an owned environment transform around a Reader-backed action.               |
| RefLocal             | `BoxRefLocal`, `RefLocal`, `SendRefLocal`; `ref_local_handler`                                                                                            | Applies a borrow-based environment transform around a Reader-backed action.         |
| Bracket              | `BoxBracket`, `Bracket`, `SendBracket`, plus Explicit-family cells; `bracket_handler`                                                                     | Runs acquire/body/release and preserves release-before-outer-continuation ordering. |
| RefBracket           | `RefBracket`, `SendRefBracket`, plus Explicit-family cells; `ref_bracket_handler`                                                                         | Rc/Arc-oriented resource lifecycle where body/release receive pointer clones.       |
| Span                 | `BoxSpan`, `Span`, `SendSpan`; `span_handler`                                                                                                             | Provides a scoped instrumentation hook around an action.                            |
| Writer listen/censor | `BoxWriterListen`, `WriterListen`, `SendWriterListen`, `BoxWriterCensor`, `WriterCensor`, `SendWriterCensor`; `writer_pre_handler`, `writer_post_handler` | Accumulates and transforms Writer output around selected actions.                   |

## Architecture Assessment

### Strengths

- The dual-row model is the right foundation. First-order operations and
  scoped around-action operations have different continuation shapes, and the
  implementation keeps those concerns separate.
- The Box/Rc/Arc split is semantically honest. Default wrappers can represent
  single-shot `FnOnce` cells; Rc wrappers represent repeatable local programs;
  Arc wrappers bake `Send + Sync` into the relevant cells instead of relying on
  accidental auto-trait propagation.
- The Explicit family gives the library a non-erased lifetime-carrying surface
  that can remain more type-directed than the default erased `Run`.
- `Run` module documentation now describes scoped rows as current
  functionality, names `im_do!` as the do-notation macro, and explains the
  default erased `Run` versus `RunExplicit` trade-off.
- The standard scoped handlers now exercise real continuation boundary
  semantics, especially for Catch, Local, Bracket, RefBracket, and Writer.
- Public standard scoped-handler docs consistently describe built-in scoped
  values as handlers, while `Dispatch*` remains reserved for internal protocol
  traits that walk handler lists or resume private boundaries.
- The macro layer shares a structural `syn::Type` row key between rows and
  handler lists, so the common macro-generated path keeps value-level and
  type-level lists aligned without relying on raw quote-string ordering for
  supported type syntax.
- Row and handler macros reject duplicate entries after structural row-key
  normalization, producing direct macro errors instead of delayed trait
  failures.
- The subsystem has been split into new-style child modules in the most
  important high-growth areas (`run`, `rc_run`, `arc_run`, Explicit boundaries,
  raw scoped handlers, standard scoped handlers).

### Limitations and Inconsistencies

- Row canonicalization is structural over the parsed type syntax the macro can
  observe, but it is still not semantic Rust type identity. Aliases, imported
  paths, and semantically duplicate spellings can still sort differently
  because proc macros cannot resolve names.
- `define_scoped_row!` rejects generic scoped rows. That is acceptable for the
  current implementation, but reusable environment/error/log-parameterized
  scoped rows will eventually need a better story.
- The builder fallback (`nt().on(...)` / scoped equivalents) prepends cells.
  Users must understand ordering if they bypass `handlers!` /
  `scoped_handlers!`; the macro path is safer.
- Default `Run` relies on erased runtime invariants and downcasts. That is an
  acceptable trade-off for the ergonomic default, but it should remain clearly
  separated from the Explicit family and covered by focused tests.
- The six-wrapper surface is large and parallel. Drift is already visible in
  documentation and naming, and future additions will amplify that unless the
  project keeps adding very focused parity checks or introduces a controlled
  generation pattern.
- Some APIs still require explicit row-minus and embedding witnesses. This is
  probably unavoidable in stable Rust for now, but it remains the main
  ergonomics cost for standard handlers such as `local_handler` and Writer
  handlers.
- Helper APIs repeat across six wrapper families. Reader helpers now exist, but
  continuing with manual per-wrapper impls for every helper would create a
  maintenance problem. `#[document_module]` now has a constrained
  `documented_helper_impls!` item marker that expands documented helper impl
  blocks before validation; the next helper family still needs to prove whether
  that marker reduces meaningful wrapper drift or whether explicit methods plus
  private support helpers are the better implementation shape.
- Some documentation examples are still closer to shape checks than
  user-facing semantic examples. The generated inventory highlights this where
  descriptions fall back to generic text or had to skip doctest snippets.

## Missing or Incomplete Areas

- Named interpreters for common effects are still incomplete compared with the
  upstream libraries. The generic `handle` machinery exists, Reader has `asks`
  and `run_reader` across all six wrappers, State has `gets`, `modify`,
  `run_state`, `eval_state`, and `exec_state` across all six wrappers, and
  default `Run`, `RcRun`, and `ArcRun` now have `run_except`. Except runners
  for the three explicit wrappers, Choose/Empty runners, and Writer-specific folds are
  still missing.
- Ergonomic helper smart constructors are incomplete relative to PureScript
  Run. Default `Run`, `RcRun`, and `ArcRun` now have `fail`, `rethrow`,
  `note`, and the Rust-facing `from_option` helper for PureScript's
  `fromJust` shape. The same Except helper set is still missing on the three
  Explicit wrappers; Writer fold helpers are also still missing.
- Macro diagnostics could be better. Duplicate row entries, duplicate
  handlers, and ordering mistakes should fail at macro expansion where
  possible.
- Generic row aliases and generic scoped-row markers are deferred. This is not
  blocking the current implementation, but it limits reusable effect stacks.
- There is no committed runtime policy for async, IO, cancellation, process
  management, or target-monad lifting. That should remain explicit before
  importing runtime-sensitive Heftia effects.

## Upstream Port Candidates

### Low-Risk / High-Value

| Candidate                              | Source                         | Why                                                                                                            |
| -------------------------------------- | ------------------------------ | -------------------------------------------------------------------------------------------------------------- |
| `fail`, `rethrow`, `note`, `from_just` | PureScript Run Except          | Thin wrappers over `throw` and `catch`; useful for common error patterns.                                      |
| `fold_writer` / named Writer runners   | PureScript Run Writer          | Fits the existing Writer plus scoped listen/censor machinery.                                                  |
| Named except/choose runners            | PureScript Run and Heftia      | Makes the generic handler machinery discoverable through common workflows.                                     |
| Heftia `Input` / `Output`              | Heftia `Input.hs`, `Output.hs` | Reduces to Reader/Writer-like semantics and should not require new runtime policy.                             |
| Heftia `Fresh`                         | Heftia `Fresh.hs`              | Reduces to State-like counter semantics once the counter type convention is chosen.                            |
| Heftia `KVStore`                       | Heftia `KVStore.hs`            | State-like map semantics; useful but needs a Rust map convention such as `BTreeMap` or user-supplied map type. |

### Worth Designing, Not Immediate

| Candidate             | Source                         | Prerequisite                                                                            |
| --------------------- | ------------------------------ | --------------------------------------------------------------------------------------- |
| `Coroutine` / `Yield` | Heftia `Coroutine.hs`          | Commit to a Rust status type and single-shot vs multi-shot continuation semantics.      |
| `Provider` / `Log`    | Heftia `Provider.hs`, `Log.hs` | Should follow decisions about Output/Writer and resource scoping rather than lead them. |

### Defer Until Runtime Policy Exists

| Candidate                                             | Source                     | Reason                                                                                                             |
| ----------------------------------------------------- | -------------------------- | ------------------------------------------------------------------------------------------------------------------ |
| `CC` / `Shift`                                        | Heftia `CC.hs`, `Shift.hs` | Answer-type-polymorphic continuation capture is a major semantic addition beyond the current scoped-handler model. |
| `Parallel`, `Timer`, `Stream`, `Subprocess`, `Unlift` | Heftia concurrent modules  | Need explicit async/executor, cancellation, IO, process lifecycle, target-monad, and `Send + Sync` policies.       |

## Recommendations

1. Add named effect runners and thin ergonomic helpers before adding more core
   effect machinery. They will make current semantics easier to exercise and
   expose where handler ergonomics are still too noisy.
2. Add natural-order manual handler builders and rename the low-level cons-list
   path around explicit `prepend` vocabulary.
3. Keep runtime-heavy Heftia ports deferred until async/IO/target-monad policy
   exists.
4. Use the generated inventory as a documentation audit queue. Rows with
   generic fallback descriptions or doctest-derived descriptions point to items
   whose local docs can be improved.

## Bottom Line

The effects system has the right long-term shape: dual rows, explicit
first-order/scoped distinction, wrapper-specific multiplicity semantics, and
private continuation-boundary protocols for around-action handlers. The main
remaining risk is not the core architecture; it is surface drift and ergonomics.
The next best work is to tighten documentation, naming, macro diagnostics, and
named helper APIs before porting runtime-sensitive effects.
